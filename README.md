Provision Nodes
    - Ensure this directory exist at the local and remote machine so you can build and deploy to either one
        - $HOME/deploy

Deploy:
```./deploy.sh```

See logs:
```journalctl -u nats.service```
